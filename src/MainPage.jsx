import React from 'react';

const MainPage = ({SignOut}) => {
    return (
        <div>
            <h1></h1>
            <button onClick={SignOut}>SignOut</button>
        </div>
    );
}

export default MainPage;
